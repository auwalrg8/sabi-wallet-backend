#[tokio::test]
async fn create_wallet_works_and_binds_device() {
    // Full integration test – will be expanded when we have test Breez mock
    assert!(true); // placeholder – real test in next issue
}