services:
  backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - BREEZ_API_KEY=${BREEZ_API_KEY}
      - DATABASE_URL=sqlite:/data/sabi.db?mode=rwc
    volumes:
      - ./data:/data
    restart: unless-stopped